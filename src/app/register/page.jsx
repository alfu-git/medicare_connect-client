import RegisterPageContent from "@/components/registerPage/RegisterPageContent";
import React from "react";

export const metadata = {
  title: "Register | MediCare Connect",
  description:
    "Sign up on MediCare Connect to access seamless healthcare services, manage appointments, connect with doctors, and take control of your health easily.",
};

const RegisterPage = () => {
  return <RegisterPageContent />;
};

export default RegisterPage;
